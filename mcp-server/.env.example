# ============================================================================
# MCP Server Environment Configuration (BSL Semantic Layer)
#
# Copy this file and rename it to `.env.dev` before running:
#    cp .env.example .env.dev
# ============================================================================

# ------------------------------
# MCP Server Runtime Settings
# ------------------------------

# Logging verbosity for server output.
# Options: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ------------------------------
# dlt Pipeline Configuration
# ------------------------------

# Name of the dlt pipeline to attach to (must match pipeline.py)
PIPELINE_NAME=contoso
