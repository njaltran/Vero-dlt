# ============================================================================
# ✅ PRODUCTION ENVIRONMENT CONFIGURATION — qdrant
#
# This file defines all environment variables required to run qdrant
# in a secure production environment. All secrets must be stored securely,
# and this file should be excluded from version control (e.g., via .gitignore).
#
# ❗ All secrets must be securely managed (e.g., via Vault or secrets manager),
# and this file must be excluded from version control (.gitignore, etc.).
#
# ➤ Copy this file and rename it to `.env` before running your project:
#    cp .env.example .env
# ============================================================================



#Admin API Key: Full access to all operations (read and write)
QDRANT__SERVICE__API_KEY=my-secure-admin-key-123

#Read-Only API Key: Limited to read operations only
QDRANT__SERVICE__READ_ONLY_API_KEY=my-secure-admin-key-123